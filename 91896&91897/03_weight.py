def weight(question):
    # getting the user's weight of their product

    while True:

        response_weight = input(question)

        if response_weight == int("What is the budget for this item: ")
        